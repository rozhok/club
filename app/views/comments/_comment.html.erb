<%= turbo_frame_tag comment do %>
  <div class="card card-lg card-border shadow-sm mt-4">
    <div class="card-body pb-2 pr-6">
      <%= comment.content %>
      <div class="card-actions justify-end">
        <%= link_to "📝Редагувати", edit_comment_path(comment, post_id: post.id), class: "btn btn-neutral btn-outline" %>
        <%= button_to "🗑Видалити", comment, method: :delete, class: "btn btn-neutral btn-outline" %>
        <%= link_to "💬Відповісти", new_comment_path(parent_id: comment.id, post_id: post.id), data: { turbo_frame: dom_id(comment, :new_comment) }, class: "btn btn-neutral btn-outline" %>
      </div>
    </div>
  </div>
<% end %>