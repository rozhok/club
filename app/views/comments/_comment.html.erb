<%= turbo_frame_tag comment do %>
  <div class="card card-lg card-border shadow-sm mt-4 <%= comment.padding %>">
    <div class="avatar absolute -left-6 top-1/2 transform -translate-y-1/2">
      <div class="w-12 rounded-full">
        <%= image_tag comment.user.avatar %>
      </div>
    </div>
    <div class="card-body pb-2 pr-6">
      <div class="prose text-black !max-w-none">
        <%= comment.content %>
      </div>
      <div class="card-actions justify-end">
        <%= link_to "📝Редагувати", edit_post_comment_path(post_id: comment.post_id, id: comment.id), class: "btn btn-neutral btn-outline" %>
        <%= button_to "🗑Видалити", post_comment_path(post_id: comment.post_id, id: comment.id), method: :delete, class: "btn btn-neutral btn-outline" %>
        <%= link_to "💬Відповісти", new_post_comment_path(parent_id: comment.id, post_id: comment.post.id), data: { turbo_frame: "comment_#{comment.id}_reply" }, class: "btn btn-neutral btn-outline" %>
      </div>
    </div>
  </div>
<% end %>
<%= turbo_frame_tag "comment_#{comment.id}_reply" %>
