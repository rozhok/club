<% if @comment.parent_id.nil? %>
  <%= turbo_stream.update "new_comment_form" do %>
    <%= render partial: "comments/form", locals: { post: @post, comment: @post.comments.build } %>
  <% end %>
  <%= turbo_stream.before Comment.new do %>
    <%= render @comment %>
  <% end %>
<% else %>
<% end %>