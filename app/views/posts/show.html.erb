<div class="container mx-auto px-4 py-4 max-w-3/4">
  <div class="card card-border shadow-sm mb-8">
    <div class="card-body prose lg:prose-xl text-black !max-w-none">
      <h1 class="!mb-0 card-title text-xl font-bold"><%= @post.title %></h1>
      <%= @post.content %>
    </div>

    <div class="card-actions justify-end p-4 pt-0 pr-6">
      <%= link_to "📝Редагувати", edit_post_path(@post.id), class: "btn btn-neutral btn-outline" %>
    </div>
  </div>

  <h2 class="text-2xl mb-8">Коментарі👇</h2>

  <%= turbo_frame_tag "new_comment_form" do %>
    <%= render partial: "comments/form", locals: { post: @post, comment: @post.comments.build } %>
  <% end %>

  <%= render @comments %>

  <%= turbo_frame_tag Comment.new %>
</div>
