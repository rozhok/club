<div class="container mx-auto px-4 py-8 max-w-3/4">
  <div class="card card-border shadow-sm mb-8">
    <div class="card-body prose lg:prose-xl text-black">
      <h2 class="card-title text-xl font-bold mb-6"><%= @post.title %></h2>
      <%= @post.content %>
    </div>

    <div class="card-actions justify-end p-4 pt-0 pr-6">
      <%= link_to "📝Редагувати", edit_post_path(@post.id), class: "btn btn-neutral btn-outline" %>
    </div>
  </div>

  <h2 class="text-2xl mb-8">Коментарі👇</h2>

  <%= turbo_frame_tag dom_id(@post, :new_comment), src: new_comment_path(post_id: @post.id) %>

  <%= turbo_frame_tag dom_id(@post, :comments), src: comments_path(post_id: @post.id) %>
</div>
